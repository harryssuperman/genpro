package com.github.harryssuperman.genpro.enums;

public enum GenProOperationObject {
    ENVIRONMENT, PROPERTY

    /* TODO
     * Add a new GenPro Operation (REPLACE) in order to replace a value for a Property in an Environment.
     * */
}
